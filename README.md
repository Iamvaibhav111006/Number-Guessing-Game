# Number Guessing Game (Java)

A console-based number guessing game where the player attempts to guess a randomly generated number.

## Features
* Random number generation
* Feedback on guesses (Too High / Too Low)
* Attempt counter and tracking

## How to Run

1. Open your terminal or command prompt in the directory containing `NGG.java`.
2. Compile the program:
